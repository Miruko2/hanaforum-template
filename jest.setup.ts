// Registers @testing-library/jest-dom matchers (e.g. toBeInTheDocument)
// for the jsdom Jest project. Only loaded by the jsdom project, not the
// node project (see jest.config.js).
import "@testing-library/jest-dom"
